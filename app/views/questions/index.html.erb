<h1>All Questions</h1>
<% @questions.each do |q| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <!-- Alternative synthax link_to q.title, q_path(q.id) -->
        <%= link_to q.title, q %>
      </h4>
    </div>
  </div>
<% end %>
